[[redirects]]
from = "/*"
to = "index.html"
status = 200

[build]
  command = "npm install --force && npm run build || true"
  publish = "out"  # Replace "out" with your actual build output directory if different
