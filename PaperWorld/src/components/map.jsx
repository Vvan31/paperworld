import React from "react";  
import map from "../assets/map.png";

const Map = () => {
    return (
        <div className="map">
            <img src={map} alt="map" />
        </div>
    );
};

export default Map;
